<div class="container">
  <h1>Flash Card Ninja!</h1>
  <p> Ninjai Profile page!</p>

  <table>
    <tr>
      <th>Username</th>
      <th>Email</th>
    </tr>
    <tr>
      <td>Your username</td>
      <td><code><%= User.find(session[:user_id]).username   %></code></td>
    </tr>
    <tr>
      <td>email </td>
      <td><code><%= User.find(session[:user_id]).email %></code></td>
    </tr>
        <tr>
      <td>The session </td>
      <td><code><%= session.inspect %></code></td>
    </tr>
  </table>
</div>
